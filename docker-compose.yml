services:
  bot:
    build: .
    container_name: portfolio-bot
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./target.yaml:/app/target.yaml:ro
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
